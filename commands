

git config --global user.email "sukhpreets25@gmail.com"
git config --global user.name "sukhpreet singh"

git init > to include our project into git 
git add [filename] | or -A   >> to add these files into staging area

